<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.festivalapp">
    <!-- Permission 추가 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 인터넷 권한 추가 : 인터넷 통신 연결 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- 위치 권한 추가 : GPS -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FestivalApp">
        <activity android:name=".activity.MainActivity"/>
        <activity android:name=".activity.InfopageActivity"></activity>
        <activity android:name=".activity.ReviewContentActivity" />
        <activity android:name=".activity.ReviewWriteActivity" />
        <activity android:name=".activity.ReviewListActivity" />
        <activity android:name=".activity.BookMarkActivity" />
        <activity android:name=".activity.MypageActivity" />
        <activity android:name=".activity.JoinActivity" />
        <activity android:name=".activity.HomeActivity" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="e06de8b2e101c412ac153c6d1854a177" /><!-- 카카오맵 키 -->
    </application>

</manifest>